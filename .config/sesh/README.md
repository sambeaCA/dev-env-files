# Sesh Configuration

Configuration for [sesh](https://github.com/joshmedeski/sesh), a smart tmux session manager.

---

### Setup

- Cache disabled for always-fresh session listings
- Default session startup command uses `eza` to display directory contents with icons and git status
- Integrated with tmux via `prefix + T` (see tmux config) for fzf-powered session switching
